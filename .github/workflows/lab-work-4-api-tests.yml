name: Lab Work 4 API Tests

on:
  push:
  pull_request:

jobs:
  api-tests:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run API integration tests
        shell: pwsh
        run: |
          & "./Lab Work №4/tests/api-tests.ps1" -Port 18081
